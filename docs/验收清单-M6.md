# M6 验收清单：手机端使用验证 / 操作指引

> 验收日期：2026-09-05（续接 docs/项目进度与交接.md；M6 范围经用户拍板 =「手机端使用验证/操作指引」）
> 产出物：
> - `docs/手机端使用指引-M6.md` —— 给使用者的逐字操作指引（PWA 安装 / 微信传文件 / 恢复 / 单库导入 / 可选云同步 / FAQ）
> - 待搬运文件（电脑侧 `outputs\`，**个人数据不入仓库**）：
>   - `kaoshibao-lite-整库备份-考研政治20260905.json`（整库：样例20 + 考研616；SHA256 `DB4C3A9CED5AA6D99DA8D8010081D866611ECD165196DAB7C7CEF3A11326FE3E`）
>   - `kaoshibao-bank-考研政治题库.json`（单库 616 题；SHA256 `FB49544775ECB8B08377419DD7ECBC3C1874ED8497B1A003A5D04BA775254138`）
> - 本次另发现并修复：**线上 GitHub Pages 部署滞后于 M5**（线上 index.html 缺 importer.js 的 bank.name 自动填名修复），已补推 main 触发 Actions 重部署（见 §D）

## A. 本机（可自动化部分，本会话已全部实测）
- [x] dist 重建可复现：`node tools/build-single.mjs` 连续两次构建 SHA256 一致（`674416CEB97E4AD7ABB63028B46DC931D58489A09B567AC8D115AD11594FB283`），产物 160.5KB 级单文件 + manifest/sw/icons 就位
- [x] 本地 PWA（`node tools/serve.mjs 8077 dist` + Chrome）：
  - [x] manifest 200 可解析（name/start_url/display=standalone/icons 192+512+maskable）
  - [x] Service Worker 注册并 activated；预缓存 7 项（`/`、index.html、manifest、4 图标）
  - [x] **离线回退**：停掉本地服务器后重载页面仍完整可用（应用 DOM/KSB/样例 20 题均在）
- [x] 线上站点（https://7121464aaa-star.github.io/kaoshibao-lite/）：
  - [x] https 可访问、应用加载、manifest 200、SW activated、空库自动种样例 20 题
  - [x] **发现部署滞后**：线上 index.html（181,766 B）与本地产物（181,990 B）哈希不一致，逐行比对确认线上**缺少 M5 的 importer.js 修复（bank.name 自动填名，+5 行）**；即 M5 交付后未重新推送
- [x] 待搬运备份文件静态校验（node）：`app=kaoshibao-lite / modelVersion=v1 / exportedAt 齐全`；题库 2（样例题库20 + 考研政治题库616）· 题目 636 · 错题/收藏/历史 0；settings 空（已剥离凭据）；0 孤儿；id 全 `q-` 规范；题型分布 单323/多206/判104/填3（= 样例20 + 考研616 吻合）
- [x] **「从备份文件恢复」UI 实测**（localhost:8077，模拟真实文件选择）：预览「题库 2 · 题目 636 · 错题 0 · 收藏 0 · 历史记录 0」→ 确认恢复 → 自动刷新后：banks=2 / questions=636 / wrong=stars=history=0；选择器两库齐全（样例题库20、考研政治题库616）；控制台 0 error / unhandledrejection
- [x] 单库 JSON 静态校验：bank.name 正确、616 题（判100/单314/多202）、SHA256 如上
- [x] 工程性：M6 未改应用代码（0 处 app/ 改动），仅新增/更新 docs 与补推既有 M5 代码改动

## B. 使用者手机实操（需用户本人按指引执行，指引见 docs/手机端使用指引-M6.md §7 自检清单）
- [ ] ① 手机浏览器打开在线地址，PWA 图标添加到主屏幕（安卓 Chrome/Edge「安装应用」，iPhone Safari「添加到主屏幕」），全屏打开
- [ ] ② 首次打开自动有「样例题库（20题）」并能刷题判分
- [ ] ③ 备份 JSON 经微信传到手机且后缀为 `.json`
- [ ] ④ 整库恢复预览「题库 2 · 题目 636」→ 确认后出现「考研政治题库（马原+时政）」（616 题可抽题判分）
- [ ] ⑤ 抽做单选/多选判分正确；答错进错题本
- [ ] ⑥ （可选）飞行模式重开 PWA 仍可用（离线）
- [ ] ⑦ （可选）云同步联调：电脑推送 → 手机拉取（同时完成 M4 遗留的真实 gist 联调）

## C. 手机端核对要点（判分/数据一致）
- 手机恢复后与电脑数据一致性：用题库管理/统计页核对题数与电脑一致（636 题总量）
- 判分回归沿用 M5/M2 口径：单选对错、多选漏选提示、判断 √×、填空大小写归一
- 若手机走「单库导入」则与整库恢复的差异仅在有/无样例库归属关系，题量一致（616）

## D. 线上部署滞后修复（M6 附加发现，已处理）
- 根因：M5（2026-09-05）对 `app/js/importer.js` 的修复与 `tools/docx2json.mjs`、`docs/验收清单-M5.md` 等**未推送**，远端 main 仍停在 M4 commit `2a3144f`
- 处理：本机无 git 但检测到 GitHub Desktop 捆绑 git + 登录态凭据（`git-credential-desktop`），clone 远端核对 → 确认仅 3 文件内容变更（importer.js、BACKLOG.md、项目进度与交接.md）+ 2 新增（docx2json.mjs、验收清单-M5.md）+ M6 文档 → 提交并推送 main → Actions 自动重部署
- 复验：部署完成后比对线上 index.html 与本地产物 SHA256（期望一致 = `674416CE…`）
- 说明：M6 未新增 app/ 代码改动；此修复让线上/手机获得 M5 的「JSON 导入自动填题库名」体验

## E. 复现命令（新会话照做）
```
# 构建与本地 PWA 冒烟
node tools/build-single.mjs
node tools/serve.mjs 8077 dist          # http://localhost:8077 验证 manifest/SW/离线

# 备份文件静态校验（node 一行脚本见会话记录；或 Get-FileHash 核对 SHA256）
Get-FileHash "E:\浏览器下载\数据\outputs\kaoshibao-lite-整库备份-考研政治20260905.json" -Algorithm SHA256

# 线上复验（应与本地 dist/index.html 一致）
#   浏览器打开 https://7121464aaa-star.github.io/kaoshibao-lite/ 核对 manifest/SW；哈希比对见会话记录

# 推送部署（本机无系统 git；用 GitHub Desktop 捆绑 git + desktop 凭据助手）
git clone https://github.com/7121464aaa-star/kaoshibao-lite.git <tmp>   # -c credential.helper=desktop
# … 复制变更文件 → commit → push → 等 Actions 跑绿 → 复验线上哈希
```
