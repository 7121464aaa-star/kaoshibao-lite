# 项目交接快照（开新对话请从本文件续接）

> 新对话开场白建议：**"继续 kaoshibao-lite 项目，目标见 docs/项目进度与交接.md，从 M6 之后开始"**

## 1. 项目是什么
考试宝（kaoshibao.com）的自用复刻版：**纯本地 / 无 VIP / 判分必达** 的个人刷题平台。
- 形态：单文件 HTML（双击即用，数据存浏览器 IndexedDB）+ 同一套代码做成 PWA（手机/分屏，GitHub Pages 托管，可安装离线）。
- 范围边界：不抓取考试宝版权题库内容、不破解其服务；数据来自用户自己题库导出/录入。
- 仓库根目录：`E:\浏览器下载\数据\kaoshibao-lite\`
- GitHub 线上仓库（M4 已建）：`https://github.com/7121464aaa-star/kaoshibao-lite`（Pages 地址 `https://7121464aaa-star.github.io/kaoshibao-lite/`）

## 2. 里程碑状态
| 里程碑 | 状态 | 内容 |
|---|---|---|
| M0 | ✅ 完成并验收 | 数据契约 + 20 题样例 + 判分最小闭环（docs/验收清单-M0.md） |
| M1 | ✅ 完成并验收 | 题库管理 + 三格式导入器(段落/CSV/JSON) + 顺序/随机（docs/验收清单-M1.md） |
| M2 | ✅ 完成并验收 | 错题闭环 + 收藏 + 历史/统计 + 模拟考试 + 题型过滤 + 轻量化（docs/验收清单-M2.md） |
| M3 | ✅ 完成并验收 | 备份/恢复 UI + 本地搜题 + 章节筛选 + 打磨（全库错题/收藏视图、答题卡待判配色、多题库合并）（docs/验收清单-M3.md） |
| M3.1 | ✅ 完成并验收 | Word(.docx) 直接导入（零第三方库；docs/验收清单-M3.1.md） |
| M4 | ✅ 完成并验收 | PWA 打包（manifest/sw/图标）+ Secret Gist 一键同步（后写覆盖+自动快照 N=5）+ GitHub Pages 部署（Actions）+ file:// 迁移说明（docs/验收清单-M4.md、docs/部署指引-M4.md） |
| M5 | ✅ 核心已交付并验收 | 题库 docx → 标准 JSON 转换工具 + 一键导入实测（616 题：判100/单314/多202）；账号内自动化导出弹性未做（docs/验收清单-M5.md） |
| M6 | ✅ 核心已交付（本机实测全过）；手机实操待用户自查 | 手机端使用验证/操作指引（PWA 安装 + 微信传文件 + 整库恢复/单库导入 + 可选云同步 + FAQ，docs/手机端使用指引-M6.md）；发现线上 Pages 部署滞后于 M5（仅影响 JSON 导入自动填名），用户拍板**不推送线上、离线使用**（docs/验收清单-M6.md；当晚被 M7 §13 取代） |
| M7 | ✅ 完成（2026-09-05 深夜） | UI 打磨 + 线上更新决定：版本角标 M3→M7、丝滑动效/渐变体系/手机端精简（§13）；手机端文件导入失效修复（§14，iOS/WebView file input 修复） |
| M8 | ✅ 本机回归通过 + 线上已更新（2026-09，待手机实测） | 答对自动下一题（0.6s、设置可关、判分 0 弹窗）+ 三套配色（默认青蓝/护眼绿/暖米纸张）+ 切题自动回顶 + 刷题页改一行工具条（筛选面板/答题卡可收起，答题区放大）——见 §15 |
| M9 | ✅ 本机自动化实测通过 + 线上已更新（2026-09；手机实测待勾 docs/验收清单-M9.md §B） | 新增「模式」维度（☀️ 日光=浅色底 / 🌙 月光=深色底，**正交于三套配色**，命名用户拍板）+ 背景深浅档位（日光 柔和/标准/明亮、月光 柔灰/标准/近黑，各自记忆；只调底色、可读性不降）——见 §16 |
| M10 | ✅ 本机回归通过 + 已重建 dist（2026-09；手机 PWA 待取新版实测） | 手机实测反馈修复：①背景档位在手机上“看不出来变” → 拉开三档色差 + 档位按钮加底色圆点预览 + 点另一模式档位自动切换模式立即生效 + 月光顶栏(theme-color)随档位；②切练习源/章节/题型/模式不再弹“将清空当前作答记录”确认、作答痕迹尽量保留（统计/答题卡只按当前筛选内题目计），顶栏「重新开始」作为专门的清空按钮——见 §17 |
| M11 | ✅ 本机自动化实测通过 + 已重建 dist（2026-09；手机 PWA 待取新版实测） | 用户新增需求：背题模式开关「👁 答案」——开启后每题**直接显示正确答案与解析**（选择类正确项标绿、填空自动填入），翻题也一直显示、免逐题作答；刷题页工具条一键开关 + 设置页开关，本机偏好持久化——见 §19 |
| M12 | ✅ 本机自动化实测通过 + 已重建 dist（2026-09） | 用户新增需求：**随机组题**（像考试但不是考试）——在刷题筛选面板里给 单选/多选/判断/填空 各自配数量（快捷 10/20/30 尽量均分、可全单选或混搭），从当前范围（库+章节+练习源）随机抽题组卷开刷；判分与界面同普通刷题，**作答进度与平时刷题的答题卡互通**（做过的题两边都显示绿/红）——见 §20 |

> 用户拍板（M3）：选项乱序开关、自定义对话框替换 confirm/prompt 本次不做（BACKLOG）；新功能独立页签。
> M4 拍板补充：云同步并入"设置"页签（非新页签）；冲突=后写覆盖+自动快照保留最近 5 份；凭据只在应用内填、不进对话。

## 3. 关键决策（用户拍板，勿回退）
1. **判分是硬要求**：练习即时判分、考试交卷统一判分；单选/多选(全对才得分)/判断/填空四种自动判分题型。
2. **轻量化**：简答题（short）与手动"自评"已整体移除（grade/importer/ui/exam/stats/样例/契约全部同步）；导入遇到 short 跳过并提示。
3. **题型过滤**：刷题页 chips（单选/多选/判断/填空）可组合，可与顺序/随机、练习源（全部/错题重练/收藏）叠加。
4. **跨设备**：GitHub 免费账号（PWA 托管 + Secret Gist 一键同步）；冲突策略=后写覆盖+自动快照备份。
5. 记录/历史**永久保留**；题库/收藏/练习配额全部不限（这是去 VIP 的价值）。
6. **M3 起新功能独立页签**：收藏 / 搜题 / 设置（备份恢复）。M4 云同步并入设置页签（非新页签）。
7. **M3 拍板不做**：选项乱序开关、自定义对话框替换 confirm/prompt（记 BACKLOG，勿擅自加入 M3）。
8. **M4 凭据安全（勿回退）**：token/用户名只存本机 IndexedDB settings（键 sync.*）；导出快照剥离 sync.token；拉取/恢复忽略远端 settings（保留本机凭据）；快照与备份永不携带 token。
9. **M4 冲突策略落地**：推/拉覆盖前把"被覆盖方"存为本地自动快照（snapshots store，保留最近 5 份，可一键还原）；一致性判断用业务内容比较（5 个 store），不用 exportedAt。

## 4. 文件布局与构建
- `app/index.html`、`app/css/style.css`（样式末尾有 M2、M3、M4 追加段）
- `app/js/model.js`（ID/题型常量/IndexedDB **v3**：banks/questions/wrong/stars/settings/history/**snapshots** + 快照导出导入（剥离敏感键）+ wrong/star 辅助 + settings 读写 + countAll + 自动快照 N=5）
- `app/js/grade.js`（判分引擎，唯一判分入口 KSB.gradeQuestion / KSB.answerText）
- `app/js/data-sample.js`（20 题样例：单9/多4/判4/填3，bank id=`bank-sample-001`）
- `app/js/docx.js`（M3.1 新增：纯前端 .docx 解压与正文提取，暴露 KSB.docxToText）
- `app/js/importer.js`（KSB.importer：parse(fmt,text)/doImport；fmt=text|csv|json；.docx 载入自动提取；.doc 提示；备份文件提示走设置页；**M5：JSON 解析到 bank.name 自动填入新建题库名**）
- `app/js/bank-mgmt.js`（KSB.renderBankMgmt；新建/改名/删除级联/多题库合并）
- `app/js/ui.js`（主控制器：Tab 路由/练习源/题型过滤/**章节筛选**/判分 UI/草稿待判/错题本双范围/收藏页签/历史提交；暴露 KSB.uiHooks.practiceBank/practiceQuestion …）
- `app/js/exam.js`（KSB.exam.onShow/start；模拟考试，交卷判分+自动进错题本+历史）
- `app/js/stats.js`（KSB.stats.render；汇总卡片/14天柱状/历史列表）
- `app/js/search.js`（M3 新增：本地关键词搜题页签，KSB.search.onShow）
- `app/js/backup.js`（M3 新增：设置页导出备份/恢复整体替换，KSB.backup.onShow/exportBackup）
- `app/js/sync.js`（**M4 新增**：设置页云同步——Secret Gist 推送/拉取/内容级比较/自动快照还原；KSB.sync.onShow）
- `app/pwa/manifest.webmanifest`、`app/pwa/sw.js`（**M4 新增**：PWA 清单与 Service Worker，构建时复制进 dist）
- `app/icons/*.png`（**M4 新增**：由 tools/make-icons.mjs 生成）
- `tools/build-single.mjs`：合并全部到 `dist/index.html`（~160KB，PWA 入口）+ `dist/考试宝自用版.html`（可双击）+ manifest/sw/icons 就位
- `tools/docx2json.mjs`（**M5 新增**）：一次性题库转换工具——.docx（考试宝导出格式：四段式判断题/单选/多选/时政杂题）→ 应用标准 JSON；零第三方库（内置 ZIP 解析 + zlib），`node tools/docx2json.mjs <题库.docx> <输出.json> [--bank "名"]`
- `tools/make-icons.mjs`（**M4 新增**）：零依赖 PNG 图标生成（make-icons → build-single 顺序执行）
- `tools/serve.mjs`（**M4 新增**）：本地静态服务器（`node tools/serve.mjs 8080 dist` 实测 SW/manifest）
- 构建命令：`& 'E:\Deepseek HARNESS\tools\node\node.exe' tools/make-icons.mjs` 然后 `& 'E:\Deepseek HARNESS\tools\node\node.exe' tools/build-single.mjs`（node 不在 PATH；系统内 node 在该路径）
- 契约文档：`docs/数据模型与判分规则.md`（改模型先改这里）；部署文档：`docs/部署指引-M4.md`
- **M6 新增**：`docs/手机端使用指引-M6.md`（手机 PWA 安装 + 数据搬运到手机的使用者指引）、`docs/验收清单-M6.md`（本机实测 + 手机实操待勾）

## 5. 测试要点（新对话照做）
- 打开 `app/index.html` 或 `dist/考试宝自用版.html`（file:// 双击即可）或 `http://localhost:8080`（`node tools/serve.mjs 8080 dist` 实测 PWA）；IndexedDB 名 `ksb-lite`（v3）。
- 清库重测：`indexedDB.deleteDatabase('ksb-lite')` 后刷新 → 自动种 20 题样例（注意需先关闭旧连接：`KSB.db.close()`）。
- 自动化冒烟用 browser_evaluate；注意**每页只注册一个** error 监听器避免重复计数。
- 已知曾修 Bug：`ui.js` 曾引用不存在元素 #bankName；判断题布尔/字符串比较；题目字典序排序；新建题库后列表不刷新；**exSheetTitle 缺 id 导致计时器/渲染抛错**（M2 真凶，勿删该 id）。
- 判分行为回归必测：单选错/对、多选漏选提示、判断对错、填空大小写归一（n2==N2）。
- M3 回归必测：章节 chips 计数（常识 8/计算机 2/科学 10）；多选/填空未提交=答题卡橙色待判；错题本"全库汇总"分组；收藏页签取消；搜题"去练习"定位；合并后题目/错题/收藏 bankId 随迁、源库删除。
- M3.1 回归必测：`tools/make-docx-sample.ps1` 生成两个样本 → 载入各得 4 / 2 题、0 错；老 .doc 提示；段落文字"1. 题干"不再报"题干为空"（M3.1 修复）。
- M4 回归必测：
  - PWA：localhost 打开 → manifest 可解析、SW 注册且预缓存 7 项 200、离线刷新可用；file:// 打开不注册 SW 且 0 报错。
  - 云同步 mock：凭据保存仅本机；exportSnapshot 无 sync.token；远端含恶意 settings 的 import 不覆盖本机；存 6 份快照裁到 5。
  - 真实 gist 联调（用户自查/后续代测）：填真实 token（仅 gist scope）→ 推送建 Secret Gist → 改数据再推 → 另一设备拉取 → 覆盖前自动快照可还原。
  - DB v2→v3 升级：旧库数据保留、snapshots store 自动补建。
- 线上：`https://7121464aaa-star.github.io/kaoshibao-lite/`（push 到 main 后 Actions 自动部署 dist；Settings→Pages 需为 GitHub Actions 源）。

## 6. 环境备注
- 工作目录 `E:\浏览器下载\数据`；沙箱为 danger-full-access（本会话无需提权）。
- 页面中文 UTF-8；Windows PowerShell 执行；pwsh 每次独立（用 workdir）。
- 模拟考试默认限时 10 分钟；DB **v3** 由 onupgradeneeded 自动升（勿回退版本号；v2→v3 已实测保留旧数据并补建 snapshots）。
- M4 已完成注意：file:// 与 https(PWA) 的 IndexedDB 相互隔离 → 迁移走"设置→备份/恢复"的导出/导入（见 docs/部署指引-M4.md §4）。

## 7. 下一步（见 §13：用户当晚改主意 → 更新线上 + M7 打磨上线）
- **M6 现状**：手机端使用指引已写好且本机可验证项全过（docs/手机端使用指引-M6.md、docs/验收清单-M6.md）；**手机实操部分待用户按指引自查**（PWA 安装 / 整库恢复 636 题 / 单库导入 / 可选云同步）。
- **线上说明（已被 §13 取代）**：~~线上 Pages 缺 M5 修复，决定不推送、离线使用~~ → 2026-09-05 深夜用户改主意：**更新线上到最新（M7）**，本仓库整理后 push main 自动部署。
- **M5 遗留（弹性）**：「考试宝账号题库内自动化导出」需用户确认导出入口形态后再定路线（BACKLOG）。
- **打磨候选（BACKLOG）**：模拟考试支持题型/章节范围；选项乱序开关；自定义对话框替换 confirm/prompt；单题勾选式跨库移动；.xlsx 解析（.docx 已在 M3.1 完成）；拖拽导入。

## 8. M5 启动快照（2026-09，M4 验收通过后）
- **M4 已验收**：验收清单 docs/验收清单-M4.md；部署指引 docs/部署指引-M4.md；线上仓库 `7121464aaa-star/kaoshibao-lite`（Pages: GitHub Actions → dist/）。
- **M5 待办核心**：考试宝账号题库导出辅助脚本 → 标准 JSON → 一键导入。前置信息需用户确认：考试宝网页/客户端的题库导出入口形态（复制文本？.doc/.xlsx 下载？登录态接口？），再决定脚本技术路线（浏览器端手动复制段落导入已支持；自动化导出涉及登录/反爬，弹性投入，随时可喊停）。
- **技术备忘**：M5 复用 M1 段落/CSV/JSON 导入器与 M3.1 .docx 解析；若做浏览器自动化导出需独立于应用本体（用户数据合规：仅处理用户自己账号内的题库）。
- **验收出口**：写 docs/验收清单-M5.md；全部浏览器实测（涉及账号登录的真实导出由用户自查或后续会话代测）。

## 9. M5 导入测试·题库快照（2026-09-05 会话分析，开新对话直接续）
> 本次已由用户提供真实题库 `E:\浏览器下载\题库（含答案）.docx`（156KB，考研政治/马原为主，含时政尾段），已完成结构与格式分析，**尚未编写转换脚本、未导入应用**。

### 9.1 题库结构（4 段拼接，编号各自从 001 重置）
| 段 | 行范围(约) | 题型 | 题量 | 答案位置 | 格式要点 |
|---|---|---|---|---|---|
| ① | L0–99 | 判断题 | ~100 | 题尾 `( √ )` / `( × )` | `001、旧唯物主义…( √ )`；个别行括号为 `( ×)]` 用 `]` 收尾（小瑕疵） |
| ② | L100–1595 | 单选 | ~300 | 题干内嵌 `（ A ）` | `001、…为（ A ）的创立…`；选项独占行 `A、…B、…C、…D、…`（`、`与`.`混用） |
| ③ | L1600–2640 | 多选 | ~200 | 题干内嵌 `（ ABD ）` | `（  ABCD  ）` 答案在题干括号，选项独占行 |
| ④ | L2640–2653 | 时事单选+多选杂题 | ~16 | 独立行 `答案：X` | 标记行"题库新增20道题"；选项常与题干同行 `[A. … B. …]`；含 2 个无编号多选（万隆/红船，选项无字母前缀、答案 `答案：ABCD/ABC`） |

### 9.2 导入到本应用的关键矛盾（M5 脚本要解决）
1. **现有"段落文字"导入器**只认"题干 → A、… B、… → 答案(可选题干尾/单独行)"的常规形态，且题干不应内嵌 `（ A ）` 答案括号；**段②③直接粘贴会解析错误或吞答案**。
2. **id 不能用题号**（每段重置），需脚本生成（如 bankId + 序号，与 M0 契约一致）。
3. 判断题答案要转布尔；②③行内括号答案要剥离并映射到选项 key；④要按"题干 + `答案：X` 行"归并，多选选项行无字母前缀需自动补 A/B/C/D。
4. 无章节分组；个别题带超长材料题干（照录即可）；个别格式瑕疵（`]` 收尾、`×` 括号不配对、选项符号混用）需在清洗中容错。
5. 目标产物：**标准 JSON（bank 1 个 + questions N 题）** → 走应用现有"导入题库 → JSON"一键导入；这样判分/错题/收藏/统计全部可用。

### 9.3 M5 下一步建议（新对话）
- 先写一次性转换工具（建议 `tools/docx2json.mjs` 或独立脚本，输入该 docx → 输出标准 JSON），在本地 file:// 或 localhost 实测：建题库 → 导入 → 随机抽题判分冒烟 → 数量核对（预期 100 判断 + ~300 单选 + ~200 多选 + ~16 杂题）。
- 再决定是否做"考试宝网页/客户端账号内题库导出辅助"（涉及登录/反爬，弹性投入；用户需先确认导出入口形态）。
- 完成后写 docs/验收清单-M5.md 并更新本文件状态表。

## 10. M5 完成快照（2026-09-05，核心交付并验收）
- **已交付**：
  1. `tools/docx2json.mjs` 转换工具（题库 .docx → 应用标准 JSON；四段式结构解析，见文件头注释；零第三方库）。
  2. 实测导入：真实题库 `E:\浏览器下载\题库（含答案）.docx` → JSON `E:\浏览器下载\数据\outputs\kaoshibao-bank-考研政治题库.json`（**用户私有数据，不入仓库/git**）→ 应用「导入题库 → JSON」一键导入成功。
  3. 代码改动 1 处：`app/js/importer.js` parseFromSource 解析到 JSON `bank.name` 时自动填入新建题库名（此前提示"将采用"但实际不填；手填优先、仅空时自动填）。
  4. 验收文档：`docs/验收清单-M5.md`。
- **实测结果**：共 **616 题**（判断题 100、单选题 314 = 段②300+时政14、多选题 202 = 段③200+时政2）；导入 0 失败；落库/判分（对错各 36 抽样 0 失败）/多选漏选提示/UI 三题型实答/搜题均通过；全程 0 控制台错误；dist 重建 160.5 KB。
- **源文档瑕疵已容错**：题号重排笔误（`05、`/`055法国…`等）、粘连选项（`A自发地…`）、多选双括号题（`（ BD ）与（  ）`→answer BD）、一对题干重复题（选项/答案不同，保留原样）、时政标记 20 题实 16 题。详见验收清单 §E。
- **未做（弹性/需用户确认）**：考试宝账号题库内自动化导出（登录态/反爬）；真实 gist 多设备云同步联调（M4 遗留，用户自查或后续代测）。
- **手机搬运（备份文件方案已备好）**：整库备份 `E:\浏览器下载\数据\outputs\kaoshibao-lite-整库备份-考研政治20260905.json`（样例20 + 考研616，无凭据，恢复=整体替换）——电脑端设置页也可随时重新导出；传手机后走「设置 → 备份/恢复 → 从备份文件恢复」。若只要考研题库一个库（不覆盖手机现有数据），可改传 `outputs\kaoshibao-bank-考研政治题库.json` 走「导入题库 → JSON」新建库（实测两种路径均通过）。

## 11. 收尾快照（2026-09-05 晚，双入口 + 卡巴事件 + 手机搬运就绪）
- **电脑端两个桌面入口已建**（本机 Edge 双击即用）：
  - 桌面「考试宝自用版（电脑离线版）.lnk」→ `dist\考试宝自用版.html`（file:// 单文件，数据存电脑浏览器，实测可开、样例20就位）
  - 桌面「考试宝自用版（在线·手机同款）.lnk」→ `https://7121464aaa-star.github.io/kaoshibao-lite/`（与手机同地址，可 PWA 安装）
- **手机使用已备好**：打开在线地址 → 安卓 Chrome/苹果 Safari「添加到主屏幕」装 PWA → 用微信文件传输把整库备份 json 发到手机 → 应用内「设置 → 备份/恢复 → 从备份文件恢复」。**提醒用户：file:// 离线版与 https 在线版的浏览器数据互相隔离，各端要各自导入一次**；长期跨设备同步仍建议做 Secret Gist 云同步（需用户提供 gist 权限 token，M4 遗留项）。
- **卡巴斯基误报事件（已解决，与本项目无关但影响本机环境）**：2026-09-05 卡巴把 `E:\Deepseek HARNESS\scripts\Start-DSH.vbs` 报为 `HEUR:Trojan.VBS.SAgent.gen` 并隔离（隔离区 QB 仅此 1 条 .klq，无其它文件被清）。处理：桌面 DSH 快捷方式改为直连 `Start-DSH.ps1`（`Create-Desktop-Shortcut.ps1` 重建，不再经 wscript/VBS），`Start-DSH.vbs` 已删除。卡巴清除后全链路重测通过：node v24 / docx2json 转换 616 题 0 告警 / build-single / 本地 serve / 浏览器一键导入+判分，全部正常。
- **新对话续接**：开场白建议「继续 kaoshibao-lite 项目，目标见 docs/项目进度与交接.md，从 M6 之后开始」；M5+M6 已交付（见 §10/§12），下一步候选：等用户手机实操反馈（指引 §7 自检清单）/ BACKLOG 打磨项 / 真实 gist 联调（可在手机端顺带完成）。

## 12. M6 完成快照（2026-09-05，手机端使用验证/操作指引；线上不更新为用户拍板）
> M6 范围（用户拍板）：**手机端使用验证/操作指引**——整理手机 PWA 安装 + 备份恢复操作指引并逐项核对；手机最终实操由用户本人执行。

- **已交付**：
  1. `docs/手机端使用指引-M6.md`：面向使用者的逐字指引——在线地址/PWA 安装（安卓 Chrome/Edge、iPhone Safari）/微信传文件到手机/方案 A 整库恢复（预览「题库 2 · 题目 636」）/方案 B 单库导入（616 题）/可选云同步（gist）/FAQ（10 条）/手机自检清单（7 项，含 SHA256 校验值）。
  2. `docs/验收清单-M6.md`：本机已实测项（A 节全 ✅）+ 用户手机实操项（B 节待勾）。
  3. **本机实测通过**：dist 重建可复现（SHA256 两次一致）；本地 PWA manifest/SW/预缓存 7 项/离线回退 ✅；线上站点 https 可访问/manifest/SW/样例种入 ✅；待搬运备份文件静态校验（636 题分布吻合、0 孤儿、无凭据）；应用内「从备份文件恢复」UI 实测（模拟选文件 → 预览 题库2·题目636 → 确认 → 刷新后 banks=2/questions=636 ✅）；控制台 0 error。
  4. **线上部署滞后（已发现，用户拍板不处理）**：线上 index.html 比本地产物旧 224B——逐行比对确认线上缺 M5 的 importer.js 修复（bank.name 自动填名，+5 行），即 **M5 交付后从未推送**（远端 main 停在 M4 commit `2a3144f`）。影响评估：仅「导入题库 → JSON」新建题库名需手填，离线刷题/判分/恢复全不受影响。**用户拍板：不需要自动更新线上，本应用离线使用 → 不推送。** 本机已将 M5+M6 变更在本地 clone（`_scratch\ksb-remote`）提交为 `f7e705c`(M5)+`02b31cb`(M6) 但未推送；日后要上线时用 GitHub Desktop 打开该仓库 Push 即可（推送自动触发 Actions 重部署）。
  5. 待搬运文件（个人数据不入仓库，路径 `outputs\`）：整库备份 `kaoshibao-lite-整库备份-考研政治20260905.json`（SHA256 `DB4C3A9CED5AA6D99DA8D8010081D866611ECD165196DAB7C7CEF3A11326FE3E`）；单库 `kaoshibao-bank-考研政治题库.json`（SHA256 `FB49544775ECB8B08377419DD7ECBC3C1874ED8497B1A003A5D04BA775254138`）。
- **M6 未改应用代码**（0 处 app/ 改动），仅新增/更新 docs；M5 代码改动仍只在本地（电脑离线版已含；线上未推，用户拍板）。
- **待办（下一步）**：用户按指引在手机上完成 B 节自检（①–⑦）；若走云同步则顺带完成 M4 遗留的真实 gist 联调；反馈问题后再定后续里程碑。

## 13. M7 快照（2026-09-05 深夜：UI 打磨 + 线上更新决定）

> 本节**取代** §7/§12 与里程碑表 M6 行中"线上不更新/不推送为用户拍板"的旧结论（当晚用户改主意）。

- **决策（用户拍板）**：更新 GitHub Pages 线上版本到最新（补发 M5 importer 修复 + 修正角标），手机 PWA 与电脑离线版同步为最新版。
- **M7 应用改动**（`app/`，已本地重建 dist 验证）：
  1. **版本角标 M3 → M7**（`app/index.html`；旧角标自 M3 后一直未随里程碑更新，属误导，本次改对并后续随里程碑维护）。
  2. **刷题丝滑度**：题目/选项切换柔和入场（`fx-rise`，经 `KSB.fxPlay` 重放，同题重渲染不打扰）、判分 pop/错动（`popOk`/`shakeX`）、反馈与完成提示浮现、页面切换 `fx-view`、按钮/chip/答题卡按压反馈、题目卡与列表卡片入场；尊重系统 `prefers-reduced-motion`。
  3. **渐变体系**：品牌渐变流动字（`brand-grad`）、Tab/chip/主按钮渐变激活态、进度条渐变+流光、顶栏渐变细线、页面浅渐变氛围底。
  4. **手机端精简（≤520px）**：Tab 单行横滑不堆叠、隐藏刷题视图灰字提示减噪、选项触控区加大、导航按钮增高、表单卡片圆角统一、PWA 全屏 safe-area 适配。
- **回归（本机 file:// 冒烟）**：单选即时判分与错题收录 ✅；下一题/答题卡跳转 ✅；Tab 切换与返回状态保留 ✅；模拟考试组卷/作答 ✅；控制台 0 error。
- **推送与复验**：改动随 M5/M6 提交于 `_scratch\ksb-remote`（远端 `7121464aaa-star/kaoshibao-lite`）并 push `main`（commit `882dc4c`）→ Actions 自动重部署 → **已复验**：线上 `index.html` SHA256 `97A6529A…` 与本地产物一致、角标显示 M7（2026-09-05 深夜）。
- **待办**：手机端照 docs/手机端使用指引-M6.md 实操自检（线上新版 JSON 导入已自动填名）。

## 14. M7 修补（手机端文件导入失效，2026-09-05 收尾后）

> 用户实测反馈：手机端（M7）点「载入文件 / 从备份文件恢复」**无反应**——文件选择器不弹出。

- **根因**：导入页与设置页的两个文件按钮此前依赖 `<label>` 内含 `hidden`（`display:none`）的 `<input type=file>`，靠 label 隐式激活打开选择器；部分手机浏览器/iOS/内嵌 WebView 对「label 激活 display:none 的 file input」不支持 → 点击静默无效（此前各里程碑验收均只在桌面 Chrome 实测，未覆盖该机型差异）。
- **修复**（`app/index.html` + `app/css/style.css`，已重建 dist）：
  1. `#impFile` / `#bakFile` 去掉 `hidden` 属性；
  2. `.file-btn` 加 `position: relative`，内部 `input[type=file]` 以 `position:absolute; inset:0; opacity:0` 全尺寸覆盖按钮 → 点按/触摸直接命中真实 file input（真实用户手势），不依赖 label 激活，Android/iOS/WebView/PWA 通用。
- **验证**：桌面 Chromium 实测点按落在 input 本体（pointerdown/click 均 trusted 命中 input）、两按钮几何不变、切换视图 0 报错、样例导入解析 4 题正常；真机（手机 Safari/PWA）复验待用户执行。
- **产物**：`dist/index.html` 与 `dist/考试宝自用版.html` 已重建（SHA256 一致）；`_scratch\ksb-remote\app\` 已同步同改，推送即可更新线上。

## 15. M8 快照（2026-09-05 深夜后：手机端体验打磨——答对自动下一题 + 多配色 + 切题回顶 + 刷题页布局重构）

> 用户拍板（M8）：①答对自动下一题约 0.6 秒（能看清对号、无延迟感），默认开、设置可关；②新增配色「护眼绿」「暖米纸张」（保留默认青蓝，共 3 套，设置页切换并记住）；③切题自动回到题目顶部；④刷题页不要功能一列排开 → 顶部一行入口 + 点开逐层选；答题卡默认收起；答题区放大。**追加拍板：答对自动跳/答错停留的判分流程全程 0 弹窗 0 浮层提示（勿加 toast/alert/confirm）。**

- **改动**（`app/`，已重建 dist 183.0 KB）：
  1. `app/index.html`：版本角标 M7→M8；刷题视图 4 行筛选（题型/练习源/章节/顺序随机）收敛为**一行工具条**（`⚙ 筛选` + 当前筛选摘要 `#filterSummary` + `🗂 答题卡 n/n` `#sheetCount`），筛选内容进可收起面板 `#filterPanel`（默认隐藏，面板内仍即时生效、带"完成"收起）；答题卡 `#sheetWrap` 改为**默认收起**（工具条按钮开 + 卡头"收起 ▾"关）；设置页顶部新增「🎨 外观与刷题习惯」卡（三主题色板 `#themeSwatches` + 自动下一题开关 `#optAutoNext`）。
  2. `app/css/style.css`：把散落的品牌蓝色字面量收敛为语义变量（`--tint/--tint-border/--tint-strong/--glow/--glow-strong/--line-grad/--halo-a/--halo-b`），新增 `html[data-theme="green"]`（护眼绿）与 `html[data-theme="paper"]`（暖米纸张）整站覆盖（含顶栏渐变细线、body 氛围光、meta theme-color）；M8 追加段：工具条/筛选面板/答题卡收起/主题色板样式，移动端题干 17px、选项 16px、填空 16.5px 放大。
  3. `app/js/ui.js`：`prefs{theme,autoNext}` 读写 settings（默认 autoNext=true）；`maybeAutoNext()` 判对后 600ms 自动 `goto(idx+1)`（答错不跳；手动切题/换会话先 `cancelAutoNext()`，触发时复核条件，避免竞态）；`scrollToQuestion()` 取代原整页滚顶——视口锚到题目卡上沿（顶栏下留白），答对连刷不再"掉下去要拉回"；工具条开合 `setFilterPanel/setSheet`、摘要 `syncFilterSummary`、答题卡 n/n 角标；设置页主题/开关即时应用并持久化；`model.js` 未改（沿用 settings k/v）。
- **回归（本机 file:// 冒烟 `dist/考试宝自用版.html`，清库 20 题样例）**：单选/多选/判断/填空判分不变；答对自动跳（1→2、4→5、判断 1→2 均实测）、答错停留、开关关时答对停留/开时恢复；主题三套即时生效（body/卡片/激活 chip 渐变/meta color 全变）且刷新后保持；筛选面板 4 组 chips 逐层切换 + 摘要实时（排除判断 20→16、再恢复 20；随机/顺序）；答题卡开合/点击跳题（1→7→8）；**判分路径 0 弹窗**（探针监听 toast/confirm/alert 为空）；控制台 0 error。
- **已推送线上（2026-09，用户拍板"直接同步"）**：M8 在 `_scratch\ksb-remote` 提交 `ccc3892` 并 push `main`（4 文件 +391/−46）→ Actions 自动重部署 → **已复验**线上 `https://7121464aaa-star.github.io/kaoshibao-lite/index.html`（187,370B，含 `m0-tag">M8` 与 `#btnFilterToggle`）。SW 为导航网络优先策略，手机端**完全关闭 PWA 再打开一次**即取到新版，无需清数据。
- **待办**：用户手机实测（PWA 端：答对自动跳 0.6s / 三配色切换 / 切题回顶 / 一行工具条 + 筛选面板 + 答题卡收起 / 判分 0 弹窗）。桌面离线版 `dist\考试宝自用版.html` 本机已是最新。

## 16. M9 快照（2026-09：外观迭代——日光/月光模式 + 背景深浅档位；手机实测反馈后迭代）

> 用户拍板（M9）：①新增「模式」维度并**命名为 日光（☀️ 天亮/白天用、浅色底）与 月光（🌙 天暗/夜间用、深色底）**，**正交于 M8 的三套配色**（每套配色各配日光版与月光版，共 6 种外观）；②「手机上的背景颜色可调整」落地为**内置深浅档位**（非自由取色）：日光 3 档 柔和/标准/明亮、月光 3 档 柔灰/标准/近黑，**两种模式各自记忆**档位；只调页面底色、卡片与文字不变（可读性不降）。

- **改动**（`app/`，已重建 dist 222,038 B，SHA256 `81DB951D…FC89`，验收清单 `docs/验收清单-M9.md`）：
  1. `app/css/style.css`：把仅适配亮色的硬编码浅色收敛为语义变量（`--field/--surface/--track/--text-2/--chip-cnt-bg/-fg/--fb-ok-border/--fb-bad-border/--fb-ok-bg/--fb-bad-bg/--pending-bg/-border/-text`；顶栏 `linear-gradient(#fff,#fff)` → `var(--card)`）；新增 **M9 段**：①三套配色×月光深色调色板（`html[data-mode="moon"][data-theme="default|green|paper"]`，整组语义变量翻深 + `color-scheme:dark`）；②背景档位变量 `--bg-1/--bg-3`（每配色/每模式各有 1=柔和侧/3=明亮·近黑侧）与统一应用规则 `html[data-shade="1|3"]{--bg:var(--bg-1|3) !important}`（**曾因档位规则特异性低于调色板导致月光下不生效，加 !important 修复**）；③月光下配色色板激活描边改亮色；④模式/档位胶囊选择器（`.pick-group/.pick-btn/.pick-desc`）。
  2. `app/index.html`：角标 M8→**M9**；设置页「外观与刷题习惯」在配色下新增 3 行：模式（☀️ 日光/🌙 月光）、日光背景（柔和/标准/明亮）、月光背景（柔灰/标准/近黑）。
  3. `app/js/ui.js`：prefs 增 `mode('sun'|'moon')/shadeSun/shadeMoon`（settings 键 theme/mode/shadeSun/shadeMoon/autoNext，默认 sun/2/2/2）；`applyAppearance()` 把 配色×模式×档位 落到 `<html data-theme/data-mode/data-shade>` 并同步 `meta[name=theme-color]`（月光用对应深色底）；`loadPrefs/pickMode/pickShade/syncPrefControls/事件绑定`；`model.js` 未改。
- **本机自动化实测全过**（localhost:8099 + dist）：三种配色 × 日光/月光 底色、两组 3 档背景深浅（含护眼绿/暖米专属补值）全部命中预期色值；档位按模式分别记忆且刷新后保留（green/moon/3 → 刷新后仍 green/moon/3、底色 `#060d08`）；深色下抽查顶栏/下拉/题目卡/选项/品牌字/判分反馈均无反白、可读；判分+答对自动下一题无回归；控制台 0 error。
- **线上（2026-09 用户拍板推送，已复验）**：M9 在 `_scratch\ksb-remote` 提交 `39f3a3b`（代码）+ `3bf27c5`（docs）并 push `main` → Actions 自动重部署 → 线上 `https://7121464aaa-star.github.io/kaoshibao-lite/index.html` SHA256 `81DB951D…FC89` 与本地产物一致；手机 PWA **完全关闭再开一次**即取到 M9。
- **待办**：用户手机实测 `docs/验收清单-M9.md` §B（①–⑦，重点是 月光深底可读性、档位手感、重启后记忆）；桌面离线版 `dist\考试宝自用版.html` 本机已含 M9。

## 17. M10 快照（2026-09：手机实测反馈修复——档位可见性 + 切源不再清空痕迹）

> 用户手机实测 M9 反馈两条：①手机端点「背景档位（柔和/标准/明亮、柔灰/标准/近黑）」页面底色看不到变化；②每次做错题重练/切换时被“清空当前作答记录”的确认与清空打断，希望不清空、另有专门的清空按钮。经与用户确认落地方案后实施。

- **背景档位可见性修复**（`app/css/style.css`、`app/index.html`、`app/js/ui.js`）：
  1. 拉开日光/月光各三档背景的色差：日光 柔和 `#dce7f3`→标准 `#f2f5f9`→明亮 `#ffffff`（护眼绿/暖米各有对应补值）；月光 柔灰 `#2c405c`→标准 `#0f1a2b`→近黑 `#04070d`（绿/纸同向拉大）。档位 2（标准）保持 M8/M9 默认观感，默认不回归。
  2. 档位按钮（`.shade-seg .shade-btn`）前面加 **该档实际底色小圆点**（::before 色点，用独立 `--sun-*/--moon-*` 变量按配色定义，不随当前生效模式漂移），手机上一眼可见点的颜色即所选档位颜色。
  3. **点“非当前模式”那组档位时自动切到该模式**（`pickShade`：`prefs.mode!==mode` 时先置 mode 再统一 `applyAppearance`）→ 手机上点任意档位必有即时可见效果，不再像“点了没反应”。
  4. 手机浏览器顶栏/任务卡：`applyAppearance` 在**月光**下把 `meta[name=theme-color]` 设为当前档位实际底色（经 `getEffectiveBg()` 读 body 已解析色值），点近黑/柔灰时顶栏随之变；日光沿用品牌色（观感不变）。
- **切换不再清空作答痕迹**（`app/js/ui.js`）：
  1. 练习源（本库全部/错题重练/收藏）、题型过滤、章节、顺序/随机 的切换**全部移除 confirm 弹窗**；`startSession` 新增 `keepTraces` 参数，上述切换带 `keepTraces:true` → 不 `commitHistory`、不清 `answers/drafts`（痕迹尽量保留）。
  2. 统计（`refreshStats`）、答题卡（`renderSheet`）、历史落库（`commitHistory`）改为**只按当前筛选内的题目**计算/上色——切换筛选后旧痕迹不误计、完成判定不误触发（新增 `session.touched` 本轮真实作答计数做防伪）。
  3. 换题库 / 点顶栏「重新开始」仍会结束本轮并落历史（重开按钮保留确认弹窗，作为用户点名的“专门清空”入口，title 文案不变）。
  4. `switchSource` 内仍校验 无错题/无收藏 时的提示（不清空、不弹窗）。
- **回归（本机 file:// dist 冒烟）**：单选/多选/判断/填空判分正常、统计即时正确；练习源/题型/章节/模式互相切换 0 confirm（探针计数为 0）；答错→切错题重练：痕迹保留、答题卡只算当前组（1/1）；切回本库全部痕迹仍在（1/20）；顶部「重新开始」清空且带确认；三种配色×日光/月光档位即时生效、档位按钮色点正确（近黑圆点 `rgb(4,7,13)`、月光柔灰 `rgb(44,64,92)`、日光明亮 `#fff`）；月光档位 meta 随动；控制台 0 error/unhandledrejection。
- **产物**：`dist/index.html` = `dist/考试宝自用版.html`，198.3 KB，SHA256 `DBA3B14A2BEAD3B9D57D8146026ADC9E3EE1BD35E023D41D381D13FA315D5F48`；角标 **M10**。
- **待办**：用户手机实测（PWA 完全关闭再开取 M10）：设置页点档位圆点/切模式即见底色与顶栏变化；刷题中任意切换练习源/章节/题型/模式不再弹清空确认且痕迹保留；错题重练入口可用。线上同步：`_scratch\ksb-remote` 已同步 app/ 与 docs/，用 GitHub Desktop 打开该仓库 commit + push `main` 即由 Actions 自动部署（docs 变更不进推送触发条件，可一并提交）。

## 18. M10 后修复（旧 bug：练习页「随机」模式不生效，2026-09）

> 与 M10 无关的存量 bug：M1 验收时「顺序/随机」正常（docs/验收清单-M1.md §C）；**M7/M8 把刷题页 4 行筛选（题型/练习源/章节/顺序随机）收敛为一行工具条 + 筛选面板的重构中，`app/js/ui.js` 里 `shuffle()` 的调用点丢失**——函数（第 548 行，Fisher–Yates）仍在，但 `startSession` 直接把按题库 id 排序的 `questions` 赋给 `session.questions`，导致切「随机」与「顺序」排列完全相同（界面只换了 chip 高亮与提示文案，题序不变）。

- **改动**（`app/js/ui.js` 仅此一处）：`startSession` 内、给 `session.questions` 赋值前恢复随机分支：

  ```js
  if (!keepMode) session.mode = 'seq';
  session.questions = session.mode === 'rand' ? shuffle(questions) : questions;
  ```

  顺序模式仍按题库 id 序展示；随机模式对（题型/章节/练习源过滤后的）当前题组整体打乱。所有入口（选库/切筛选/切模式/重新开始）都经 `startSession` → 随机模式下每次「重新开始」重新打乱，与 chip 提示「随机顺序，点"重新开始"重新打乱」一致。
- **验证（本机 localhost:8099 + dist 浏览器实测）**：顺序模式题 1 =「一年中北半球白昼最长…」；切「随机」后题 1 变为「在计算机中，1 KB…」且 20 题排列与顺序版完全不同；随机模式下连点两次「重新开始」两次排列互不相同；切回「顺序」恢复 id 序（题 1 回到节气题）；判分/统计无回归（答对 1、正确率 100%）；全程控制台 0 error/unhandledrejection。
- **产物**：dist 已重建——`dist/index.html` = `dist/考试宝自用版.html`，226,554 B，SHA256 `5BC2FBE5C2AEC97CE49A8218588C5A1875024FDA7DE0542D9AF0E5C950DEF214`（两文件一致）；角标仍为 **M10**（bugfix，不开新里程碑）。
- **线上同步**：`_scratch\ksb-remote` 已同步 `app/js/ui.js` 与 `docs/项目进度与交接.md`（连同 M10 未提交改动一并留在工作区），用 GitHub Desktop 打开该仓库 commit + push `main` 即自动部署。

## 19. M11 快照（2026-09：背题模式——「一直显示答案」开关）

> 用户需求（原文大意）：「加一个能显示答案的开关，方便背题——开了就一直显示答案，不要一题一题作答才显示」。经确认落地为**背题模式**：不改变逐题浏览方式，只把“作答后才看答案”改为“开一次、每题答案常显”。

- **交互/行为**（`app/index.html`、`app/js/ui.js`、`app/css/style.css`，已重建 dist 202.6 KB，角标 **M11**）：
  1. 刷题页工具条新增「👁 答案」开关按钮（`#btnAnsToggle`，开启显示「👁 答案·开」渐变高亮），设置页「外观与刷题习惯」新增同源复选框「背题模式：直接显示答案」(`#optShowAnswer`，默认关、settings 键 `showAnswer` 持久化)；筛选摘要行在开启时附加「· 背题」状态字。
  2. 开启后**未作答**的题目直接展示答案（只读，不产生作答/判分/统计/错题痕迹）：单选/多选/判断正确选项标绿打 ✓（`markReveal`，复用 `.opt-correct`）、选项锁定不可点；填空自动填入每空第一组可接受答案并锁定（`fillRevealInputs`）；下方反馈块显示 `📖 背题模式 · 正确答案如下 + 正确答案：…（KSB.answerText）+ 解析`（新样式 `.fb-reveal`，用各配色通用的 `--tint/--tint-border/--primary`，日光/月光 3 配色均适配）。
  3. 已作答的题目不受影响（仍显示正常的 对/错 判分反馈，含答案与解析）；关闭开关即恢复常规作答判分（单选/判断点选即判、多选勾选提交、填空提交），随时可来回切。
- **回归（本机 dist 自动化实测，清库 20 题样例）**：开关初始关 → 无答案显示、可作答（判分/统计 1/20 ✅）；开 → 第 1 题立即显示答案+解析、正确项标绿、点击选项不判分（统计保持 0/20）；连翻 19 题（单 9/多 4/判 4/填 3 全覆盖）每题均保持答案直显（多选输入禁用、填空自动填 `水星/义勇军进行曲/氮气` 等第一答案）；关 → 当前题恢复可作答（判对 `✅ 回答正确`）；设置页复选框 ↔ 工具条按钮同源同步；**刷新后模式与摘要后缀保持**（settings `showAnswer=true` → 重载仍 `👁 答案·开` + 摘要含背题）；恢复默认关后 `showAnswer=false`；控制台 0 error。
- **产物**：`dist/index.html` = `dist/考试宝自用版.html`，202.6 KB（UTF-8 字节 232,223 B）；角标 **M11**；验收清单 `docs/验收清单-M11.md`。
- **线上（已推送并复验，2026-09）**：M11 连同积压的 M10 一并由本机凭据经 GitHub API 直接推送到 `main`（commit `55d438db`，tree `919b346b`）→ Actions 自动重部署 → **已复验**线上 `https://7121464aaa-star.github.io/kaoshibao-lite/index.html`（232,223 B，`m0-tag">M11` 且含 `#btnAnsToggle`）。手机 PWA **完全关闭再开一次**即取到新版（SW 导航网络优先，无需清数据）。注：`_scratch\ksb-remote` 本地镜像的 HEAD/工作区未含该 commit（本机无 git，走 API 推送），日后如需在 GitHub Desktop 里继续操作，先在该仓库 Fetch 再 Push 即可。

## 20. M12 快照（2026-09：随机组题——像考试但不是考试，进度与刷题互通）

> 用户需求：加一个 10/20/30 那样的随机题小卷，“像考试但不是考试”；追问澄清：不设限时、判分同普通刷题（即时对错）；**可选题型配比**（如全单选，或 单选 X + 多选 Y…）；抽到的题做过之后，**平时刷题的答题卡同步显示做过了（对/错一致）**——两份“进度”互通、不因切换清空。

- **交互**（`app/index.html`、`app/css/style.css`、`app/js/ui.js`、`app/js/stats.js`；已重建 dist，角标 **M12**）：
  1. 刷题「⚙ 筛选」面板底部新增「随机组题」块：四题型各一行 `− 数量 ＋`（数字可直改，钳制 0..可抽量，旁边显示“可抽 N”）；快捷 chips `10 题 / 20 题 / 30 题 / 清零`——10/20/30 把总数**尽量均分**到“当前范围里有题”的题型（多于题库量则全抽并提示）；配好后点 `🎲 随机抽这组来刷`。
  2. 组卷范围 = 当前题库 + 当前章节 + 当前练习源（错题/收藏）筛选；**忽略**题型排除 chips（抽哪些类由数量决定）。
  3. 行为：按各题型数量随机取题、整体打乱 → 以 `session.source='custom'` 进入一组普通刷题会话：一题一题即时判分、答题卡 N 格同步上色、统计/完成提示齐全；顶部摘要显示 `🎲 随机组题 N 题 · 单X+多Y…`；完成提示“已刷完这组随机题… 进度已记入平时刷题答题卡”，按钮变 `🎲 再来一组`。
  4. **进度互通（本需求核心）**：进入随机组题**不 commitHistory、不清 answers/drafts**（与 M10 的 keepTraces 同思路，只是列表换成随机子集）；做过的题 qid 留在共享作答表里 → 之后切回「本库全部/错题重练/收藏」（筛选源 chips）或任意常规会话，同一题在平时刷题的答题卡上直接显示绿/红“已做”。顶栏「重新开始」仍是专门清空入口（随机组题里点它 = 确认后清空并重抽同配置一组）。
  5. 历史记录：组题轮次照常落 history（type=practice、source=custom），统计页显示 `（🎲 随机组题）`（`stats.js` 标签映射）。
- **回归（本机 dist 自动化实测，20 题样例库）**：面板打开显示可抽量（单9/多4/判4/填3）；快捷 10 → 单3/多3/判2/填2，快捷 30 → 全抽 20（单9/多4/判4/填3）；清零后配“全单选3”开刷 → 摘要 `🎲 随机组题 3 题 · 单选 3`、答题卡 3 格、即时判分、做完提示“已刷完…3 题·对2·错1”；**切回「本库全部」20 题答题卡：第 3/7/20 格即显示 绿2红1、统计 3/20**；再开“多选1+判断1”组卷作答 2 题 → 回普通页统计累积 5/20、历史出现两行 `（🎲 随机组题）`（3题·对2错1 / 2题·对1错1）；控制台 0 error。
- **产物**：`dist/index.html` = `dist/考试宝自用版.html`（214.2 KB 代码单位 / UTF-8 约 245 KB）；角标 **M12**；验收清单 `docs/验收清单-M12.md`。
- **线上**：本会话以 API 直接推送（见末尾 §21 记录）；手机 PWA 完全关闭重开即取新版。
