# M5 验收清单：题库 docx → 标准 JSON → 实测导入

> 验收日期：2026-09-05（续接 docs/项目进度与交接.md §9 的题库快照）
> 输入文件：`E:\浏览器下载\题库（含答案）.docx`（156KB，考研政治/马原为主，尾含时政段）
> 转换工具：`tools/docx2json.mjs`（已入库，零第三方库；Node ≥18，内置 ZIP 解析 + zlib）
> 产物 JSON：`E:\浏览器下载\数据\outputs\kaoshibao-bank-考研政治题库.json`
> 用途：本题库为个人自用数据，**不放入仓库、不入 git**；需要时用工具按需重新生成。

## A. 转换工具（tools/docx2json.mjs）
- [x] 直接读 .docx（ZIP 中央目录 + deflate 解压，逻辑同 app/js/docx.js 的浏览器端，零第三方库）
- [x] 用法：`node tools/docx2json.mjs <题库.docx> <输出.json> [--bank "题库名"]`
- [x] 识别四段结构：段①判断题（句尾 √/× 括号）→ 段②单选（题干内嵌 （ A ））→ 段③多选（题干内嵌 （  ABD  ））→ 段④时政杂题（"题库新增20道题"标记后，按"答案：X"行分块）
- [x] 题干内嵌答案括号（纯 A-H 字母）→ 剥离并转成 answer；原位替换为全角空括号（　），**不在题干泄露答案**
- [x] 判断题 （ √ ）/（ × ）→ answer: true/false，括号从题干剥离
- [x] 题号前缀剥离：`001、`/`05、`/`7.`/`16.`/粘连笔误 `055法国…`/`089党的二十大…`（4 位年份开头的题干不受误伤）
- [x] 选项规范化：`A、`/`A.`/`A．`/`A)` 及粘连笔误 `A自发地…` → key/text
- [x] 时政段：题干同行内联选项（`7. …？ A. 蒋万安 B. …`）与分段选项均支持；万隆/红船无字母前缀选项按 A..D 自动补键
- [x] 自校验：单选答案键 ∈ 选项、多选答案 ⊆ 选项键、判断答案布尔、题干非空；异常告警并在控制台列出

## B. 转换产物核对（616 题）
- [x] 数量：判断题 100 + 单选题 314 + 多选题 202 = **616**（≈ §9 预估 100+~300+~200+~16）
  - 段②单选 300 题（含题号笔误行 055/057/089 均正确成题）
  - 段③多选 200 题（含粘连选项 `A自发地…`、双括号题 `（ BD ）与（  ）` 均正确）
  - 段④时政 16 题（14 单选 + 万隆/红船 2 多选）
- [x] 判断题答案分布 60 真 / 40 假；与源文档 √× 数一致
- [x] 字段齐全：type/stem/options/answer；无 short/fill 混入；题干无残留 `（ A ）` 样式答案标记
- [x] JSON 结构：`{ "bank": {"name": "考研政治题库（马原+时政）"}, "questions": [...] }` —— 应用 JSON 导入器可直接识别
- [x] id/createdAt 不预生成（由应用导入时按 M0 契约分配，避免 id 冲突）

## C. 应用内实测导入（真实 UI，localhost:8077 运行 dist/ 单文件版）
- [x] 清库（`indexedDB.deleteDatabase('ksb-lite')`）后刷新 → 自动种 20 题样例题库
- [x] 导入页签 → JSON 源格式 → 粘贴/拉取 JSON → 解析预览：**成功 616 道（单选 314 · 多选 202 · 判断 100），0 失败**
- [x] **一键导入**：JSON 内 bank.name「考研政治题库（马原+时政）」自动填入新题库名（本次顺带修复：importer.js 解析到 bankName 时自动填入 impNewName，此前提示"将采用"但不会自动填）
- [x] 确认导入 → toast「已导入 616 道 → 考研政治题库（马原+时政）」
- [x] 落库核对：banks 2 个（样例 + 新库）；新库 616 题，类型分布 judge=100/single=314/multiple=202；id 全部 `q-…` 规范、createdAt 齐全；0 孤儿
- [x] 判分引擎冒烟：judge/single/multiple 各 12 题随机抽样 —— 正确作答全部判对、错误作答全部判错（36/36）
- [x] 多选漏选提示：漏选一项 → detail 返回「漏选了 D」类提示正常
- [x] UI 实答冒烟：判断题点"正确/错误"、单选点选项、多选勾选+「提交本题」→ 三题型反馈均「✅ 回答正确」，统计栏/错题本联动正确（答错 1 题进错题本后已清理）
- [x] 搜题页签：关键词「唯物史观」命中 19 题，列表可渲染（去练习按钮存在）
- [x] 全程 0 控制台 error / unhandledrejection
- [x] 冒烟产生的 history/wrong/stars 已清理，验收后库为：样例题库 20 题 + 新库 616 题，history=0/wrong=0/stars=0

## D. 工程性
- [x] `node tools/build-single.mjs` 通过：dist/index.html（160.5 KB）+ 考试宝自用版.html + PWA 资源就位
- [x] 唯一代码改动：importer.js parseFromSource 中 bankName 自动填入新库名（约 +5 行），行为向后兼容（用户手填优先、仅空时自动填）
- [x] 数据模型契约无变更（产物是符合 M0 的 Question；导入仍走现有 JSON 导入器/落库逻辑）

## E. 已知边界（可接受 / 记录）
- 源文档自身数据瑕疵已容错并如实保留：
  1. 单选/多选**题号重排且笔误**（`05、`、`055法国…`、`057对…`、`089党的二十大…`）——按行结构成题，不依赖题号连续
  2. 单选带**超长材料题干**的题（如 055、089、196 等）照录单段题干
  3. 多选 Q176 `（ BD ）与（  ）` 源文档即两个括号，answer=BD、题干保留双空位；判断其语义为「经济效益(B)+社会效益(D)」
  4. 题干重复 1 对：`下列关于科学技术的说法错误的是（　）。` 两道题选项/答案不同（C 与 D）——源文档本身如此，未做去重（保留原样便于对答案）
  5. 时政段标记「题库新增20道题」但实际含答案行为 16 题（源文档即如此）
- 卷内不含填空/简答题（符合轻量版边界）；无章节字段（源无分组）
- 本批只做「题库 docx → JSON → 导入」；「考试宝账号内自动化导出（登录态/反爬）」仍弹性未做 → 记入 BACKLOG，需用户确认导出入口形态后再定技术路线

## F. 自动化实测记录（Chrome via DSH browser，localhost:8077 dist 单文件版）
| 项 | 结果 |
|---|---|
| 转换：docx2json.mjs 一次通过 | ✅ 616 题、0 告警 |
| 产物核对：类型分布/答案合法/题干非空/无残留答案括号 | ✅ 0 问题 |
| 解析预览（UI） | ✅ 成功 616、失败 0 |
| 一键导入（bank.name 自动填名） | ✅ 已导入 616 道 → 「考研政治题库（马原+时政）」 |
| 落库：bank/questions 数量、id、createdAt、孤儿 | ✅ 2 banks / 616 题 / 0 异常 |
| 判分冒烟（gradeQuestion ×36：对/错各半） | ✅ 0 失败 |
| 多选漏选 detail | ✅「漏选了 D」 |
| UI 实答 judge/single/multiple | ✅ 3/3 回答正确 |
| 搜题「唯物史观」 | ✅ 19 题 |
| 控制台 error/unhandledrejection | ✅ 0 |
| dist 重建 | ✅ 160.5 KB 单文件 + PWA 资源 |

## G. 复现命令（新会话照做）
```
node tools/docx2json.mjs "E:\浏览器下载\题库（含答案）.docx" "E:\浏览器下载\数据\outputs\kaoshibao-bank-考研政治题库.json" --bank "考研政治题库（马原+时政）"
node tools/build-single.mjs
node tools/serve.mjs 8077 dist   # 浏览器开 http://localhost:8077 → 导入题库 → JSON → 拉取上述 JSON → 解析 → 确认导入
```
